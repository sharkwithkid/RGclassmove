# 오류
폴더 경로 오류 메세지 수정
파일이 존재하지 않는 경우, 두 개 이상 존재하는 경우, 확장자가 맞지 않는 경우, 폴더 구조가 맞지 않는 경우 등을 나누어 안내해야 함.

파일 스캔할 때, 확장자가 xlsx가 아니면 무조건 형식 이야기로 반환.
키워드 이야기를 동시에 하니까 헷갈림
확장자가 맞지 않으면 일단 형식 이야기부터 하고, 그 다음에 키워드를 찾는 것,
그 다음에 전입/전출 명단이 있는데 명부가 있고 이런 논리 구조를 따지기.
[오류] 학생명부(.xlsx, 파일명에 '학생명부') 파일을 찾지 못했습니다. < 이런 멘트도 사용자에게 왜 명부가 필요한지를 안내할 필요가 있음





# 엑셀 서식
모든 엑셀 서식은 입력한 그대로 존재하도록. 3-1을 입력했을 때 날짜로 변환되는 등의 일은 있어서는 안 됨


# 헤더
[ERROR] [오류] 신입생 파일 2행에서 학년(B), 반(C), 성명(E)은 필수입니다.
하드코딩하지 않기로 하고 헤더는 자동 감지하는 것으로 했는데...